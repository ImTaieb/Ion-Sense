{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Standard desktop permissions for the user-opened settings window.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
